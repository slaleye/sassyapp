{
  "name": "sassyapp",
  "version": "1.0.0",
  "description": "Sass app configured with gulp to run on a live server",
  "main": "index.js",
  "scripts": {
    "start": "gulp start-web-server",
    "build": "gulp",
    "dev": "npm-run-all -p build start"
  },
  "author": "slaleye",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-sass": "^4.0.1",
    "gulp-webserver": "^0.9.1",
     "npm-run-all": "^4.1.3"
  }
}
